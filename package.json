{
  "name": "stagecall-dance-scheduler",
  "version": "1.0.0",
  "description": "Dance competition schedule manager with real-time sync, notifications, and media sharing",
  "main": "server.js",
  "scripts": {
    "build:css": "tailwindcss --config tailwind.config.js --input tailwind.input.css --output public/tailwind.css --minify",
    "start": "node server.js",
    "setup": "npm install && npm run build:css"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.6.0",
    "tailwindcss": "^3.4.0",
    "uuid": "^9.0.0"
  }
}